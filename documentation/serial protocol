sets:
sf1 [freq]
sf2 [freq]
sp1 [phase]
sp2 [phase]
sfo [1/2/m(modulation)]                      #frequency output
spo [1/2/m(modulation)]                      #phase output
so [o(off)/s(sine)/t(triangle)/q(square)]    #output mode
smf [freq]                                   #modulation freq

gets:
gsf1 (returns frequency 1)
gsf2 
gp1  (returns phase 1)
gp2

etc...